# Sales Performance Dashboard

A resume-ready project demonstrating SQL + Python analysis and a Tableau/Power BI dashboard over ~10,000 transactions across 4 regions. You can run fully locally (SQLite) and reproduce all metrics and visualizations.

## 🧭 Project Goals
- Clean and model transaction data
- Identify revenue leakage (e.g., loss-making SKUs/regions, discount issues)
- Surface seasonal patterns and category performance
- Publish an interactive dashboard with clear KPIs

## 📂 Repository Structure
```
Sales-Performance-Dashboard/
├─ data/
│  ├─ raw/                # put original csv here (transactions.csv, products.csv, regions.csv)
│  └─ processed/          # cleaned CSVs generated by notebooks/src
├─ sql/                   # SQL scripts (schema + analysis queries)
├─ src/                   # python modules (ETL/feature engineering)
├─ notebooks/             # Jupyter notebooks for EDA & modeling
├─ dashboards/            # export your Tableau .twbx or Power BI .pbix here
├─ reports/               # PDF/PNG of dashboard and a 1-page executive summary
└─ .github/workflows      # CI to test that the project runs
```

## 🗃️ Data (example schema)
- **transactions.csv**: `order_id, order_date, sku, region, qty, unit_price, discount, channel`
- **products.csv**: `sku, product_name, category, sub_category`
- **regions.csv**: `region, manager, currency`

> Use any public dataset or generate a synthetic one. Keep ~10–50k rows for realism.

## ⚙️ Quickstart
1) Create and activate a virtual environment, then install dependencies:
```bash
pip install -r requirements.txt
```
2) (Optional) Build a local SQLite DB from CSVs:
```bash
python src/build_sqlite.py
```
3) Explore the data:
```bash
jupyter notebook notebooks/EDA_and_Leakage.ipynb
```
4) Export curated tables to `data/processed/` for dashboarding.
5) Build your dashboard in Tableau/Power BI and export to `dashboards/`.

## 🔍 Analysis Outline
- **KPI set**: Revenue, Margin, AOV, Units, Discount %, Return Rate (if available)
- **Leakage checks**:
  - SKUs/regions with high discount, low margin (if COGS exists), or negative trends
  - Excessive discounting on specific SKUs or channels
  - Stockouts or missing SKUs (if data available)
- **Seasonality**: Monthly revenue trend, category seasonality
- **Segmentation**: Top N products/regions, Pareto 80/20

## 📊 Dashboard (suggested pages)
1. **Executive Overview** — Revenue, Margin, Growth vs. prior period
2. **Products & Categories** — Top/Bottom SKUs, revenue leakage table
3. **Regions** — Heatmap by region/category
4. **Time** — Seasonality and YoY/MoM trend

## 🧪 Reproducibility
A minimal CI workflow (GitHub Actions) is included to lint and run a smoke test so recruiters can see the repo is healthy.

## 🗺️ Deliverables for your resume
- “Built an interactive Tableau dashboard analyzing 10k+ transactions across 4 regions; identified revenue leakage and seasonal patterns; recommended actions projected to lift revenue by 10%.”

---
*Last updated: 2025-08-20*
